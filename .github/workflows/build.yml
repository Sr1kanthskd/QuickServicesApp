name: Build Android App From Zip

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        # Cache removed so it stops crashing!

    - name: Unzip and Flatten Project
      shell: python
      run: |
        import zipfile
        import os
        import shutil

        zip_files = [f for f in os.listdir('.') if f.endswith('.zip')]
        if not zip_files:
            print("Error: No zip file found!")
            exit(1)
        
        zip_name = zip_files[0]
        temp_dir = "temp_extract"
        with zipfile.ZipFile(zip_name, 'r') as zip_ref:
            zip_ref.extractall(temp_dir)

        # Find the actual folder containing the Android project
        target_source = temp_dir
        for root, dirs, files in os.walk(temp_dir):
            if 'build.gradle' in files or 'build.gradle.kts' in files:
                target_source = root
                break

        print(f"Moving files from {target_source} to root...")
        for item in os.listdir(target_source):
            s = os.path.join(target_source, item)
            d = os.path.join('.', item)
            if os.path.isdir(s):
                if os.path.exists(d): shutil.rmtree(d)
                shutil.copytree(s, d)
            else:
                shutil.copy(s, d)

        shutil.rmtree(temp_dir)
        os.remove(zip_name)
        print("Extraction complete!")

    - name: Generate Missing Gradle Wrapper
      run: |
        # If AI Studio forgot the gradlew file, generate a fresh one natively!
        if [ ! -f "gradlew" ]; then
          echo "gradlew not found. Generating a new one..."
          gradle wrapper
        fi
        chmod +x gradlew

    - name: Build APK and AAB
      run: |
        ./gradlew assembleRelease --stacktrace
        ./gradlew bundleRelease --stacktrace

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: QuickServices-APK
        path: "**/build/outputs/apk/release/*-unsigned.apk"

    - name: Upload AAB
      uses: actions/upload-artifact@v4
      with:
        name: QuickServices-AAB
        path: "**/build/outputs/bundle/release/*.aab"
        
